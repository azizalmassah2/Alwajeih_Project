<UserControl x:Class="Alwajeih.Views.Management.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="RightToLeft"
             FontFamily="{StaticResource Tajawal}"
             FontSize="14"
             xmlns:vm="clr-namespace:Alwajeih.ViewModels.Management">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="/Resources/Styles/Buttons.xaml" />
                <ResourceDictionary Source="/Resources/Styles/DataGrids.xaml" />
                <ResourceDictionary Source="/Resources/Styles/ScrollBarStyles.xaml" />
                <ResourceDictionary Source="/Resources/Styles/MainStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <UserControl.DataContext>
        <vm:AuditLogViewModel/>
    </UserControl.DataContext>
    <Grid Background="#F8F9FE">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20,20">
            <TextBlock Text="📝" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <TextBlock Text="سجل التدقيق" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>
        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="20" Margin="20,0,20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" MinWidth="150"/>
                    <ColumnDefinition Width="Auto" MinWidth="110"/>
                    <ColumnDefinition Width="Auto" MinWidth="110"/>
                    <ColumnDefinition Width="Auto" MinWidth="80"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding SearchText}" Height="45" Padding="12,8" Margin="0,0,10,0"/>
                <DatePicker Grid.Column="1" SelectedDate="{Binding StartDate}" Height="45" Margin="0,0,10,0"/>
                <DatePicker Grid.Column="2" SelectedDate="{Binding EndDate}" Height="45" Margin="0,0,10,0"/>
                <Button Grid.Column="3" Command="{Binding FilterCommand}" Content="🔍 تصفية" Height="45"/>
            </Grid>
        </Border>
        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="20" Margin="20,10,20,20">
            <DataGrid ItemsSource="{Binding AuditLogs}" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      Style="{StaticResource ModernDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="التاريخ" Binding="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm}" Width="Auto" MinWidth="120"/>
                    <DataGridTextColumn Header="المستخدم" Binding="{Binding UserName}" Width="Auto" MinWidth="100"/>
                    <DataGridTextColumn Header="الإجراء" Binding="{Binding ActionArabic}" Width="Auto" MinWidth="80"/>
                    <DataGridTextColumn Header="النوع" Binding="{Binding EntityTypeArabic}" Width="Auto" MinWidth="100"/>
                    <DataGridTextColumn Header="التفاصيل" Binding="{Binding Details}" Width="*" MinWidth="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
