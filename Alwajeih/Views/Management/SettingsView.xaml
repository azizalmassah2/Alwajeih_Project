<UserControl
  x:Class="Alwajeih.Views.Management.SettingsView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  FlowDirection="RightToLeft"
  FontFamily="{StaticResource Tajawal}"
  FontSize="14"
>
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Resources/Styles/Colors.xaml" />
        <ResourceDictionary Source="/Resources/Styles/Buttons.xaml" />
        <ResourceDictionary Source="/Resources/Styles/MainStyles.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>
  
  <Grid Background="#F8FAFC">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    
    <!-- العنوان -->
    <StackPanel Grid.Row="0" Margin="30,25,30,20">
      <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
        <TextBlock Text="⚙️" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center" />
        <TextBlock
          Text="الإعدادات والإدارة"
          FontSize="22"
          FontWeight="Bold"
          Foreground="#1E293B"
          VerticalAlignment="Center"
        />
      </StackPanel>
      <TextBlock
        Text="إدارة النظام والمستخدمين والنسخ الاحتياطي"
        FontSize="13" Foreground="#64748B" Margin="34,0,0,0"
      />
    </StackPanel>
    
    <!-- التبويبات -->
    <TabControl Grid.Row="1" Margin="30,0,30,25" Background="Transparent" BorderThickness="0">
      <TabControl.Resources>
        <Style TargetType="TabItem">
          <Setter Property="Template">
            <Setter.Value>
              <ControlTemplate TargetType="TabItem">
                <Border x:Name="Border" Background="White" BorderBrush="#E2E8F0" BorderThickness="1,1,1,0" 
                        CornerRadius="12,12,0,0" Padding="24,12" Margin="0,0,8,0">
                  <ContentPresenter ContentSource="Header" />
                </Border>
                <ControlTemplate.Triggers>
                  <Trigger Property="IsSelected" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="White" />
                    <Setter TargetName="Border" Property="BorderBrush" Value="#2563EB" />
                    <Setter TargetName="Border" Property="BorderThickness" Value="2,2,2,0" />
                  </Trigger>
                  <Trigger Property="IsSelected" Value="False">
                    <Setter TargetName="Border" Property="Background" Value="#F8FAFC" />
                  </Trigger>
                </ControlTemplate.Triggers>
              </ControlTemplate>
            </Setter.Value>
          </Setter>
        </Style>
      </TabControl.Resources>
      
      <!-- تبويب إعدادات النظام -->
      <TabItem>
        <TabItem.Header>
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="⚙️" FontSize="16" Margin="0,0,8,0" />
            <TextBlock Text="إعدادات النظام" FontSize="15" FontWeight="SemiBold" />
          </StackPanel>
        </TabItem.Header>
        <Frame x:Name="SystemSettingsFrame" NavigationUIVisibility="Hidden" />
      </TabItem>
      
      <!-- تبويب النسخ الاحتياطي -->
      <TabItem>
        <TabItem.Header>
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="💾" FontSize="16" Margin="0,0,8,0" />
            <TextBlock Text="النسخ الاحتياطي" FontSize="15" FontWeight="SemiBold" />
          </StackPanel>
        </TabItem.Header>
        <Frame x:Name="BackupFrame" NavigationUIVisibility="Hidden" />
      </TabItem>
      
      <!-- تبويب إدارة المستخدمين -->
      <TabItem>
        <TabItem.Header>
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="👥" FontSize="16" Margin="0,0,8,0" />
            <TextBlock Text="إدارة المستخدمين" FontSize="15" FontWeight="SemiBold" />
          </StackPanel>
        </TabItem.Header>
        <Frame x:Name="UserManagementFrame" NavigationUIVisibility="Hidden" />
      </TabItem>
      
      <!-- تبويب سجل التدقيق -->
      <TabItem>
        <TabItem.Header>
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="📜" FontSize="16" Margin="0,0,8,0" />
            <TextBlock Text="سجل التدقيق" FontSize="15" FontWeight="SemiBold" />
          </StackPanel>
        </TabItem.Header>
        <Frame x:Name="AuditLogFrame" NavigationUIVisibility="Hidden" />
      </TabItem>
    </TabControl>
  </Grid>
</UserControl>
