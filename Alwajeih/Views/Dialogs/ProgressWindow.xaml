<Window x:Class="Alwajeih.Views.Dialogs.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="معالجة البيانات"
        Height="320"
        Width="520"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        FlowDirection="RightToLeft">
  
  <Window.Resources>
    <!-- تحميل خط Tajawal -->
    <FontFamily x:Key="Tajawal">pack://application:,,,/Resources/Fonts/#Tajawal</FontFamily>
  </Window.Resources>
  
  <Border Background="White" 
          CornerRadius="16" 
          Padding="40"
          BorderBrush="#E2E8F0"
          BorderThickness="1">
    <Border.Effect>
      <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="0" Opacity="0.4"/>
    </Border.Effect>
    
    <StackPanel>
      <!-- أيقونة ونص العنوان -->
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,25">
        <TextBlock Text="📊" FontSize="28" Margin="0,0,10,0"/>
        <TextBlock Text="جاري معالجة البيانات..." 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Foreground="#1E293B"
                   VerticalAlignment="Center"
                   FontFamily="{StaticResource Tajawal}"/>
      </StackPanel>
      
      <!-- شريط التقدم -->
      <ProgressBar x:Name="ProgressBar"
                   Value="0" 
                   Maximum="100" 
                   Height="35" 
                   Margin="0,0,0,20"
                   Background="#F1F5F9"
                   Foreground="#8B5CF6"
                   BorderBrush="#E2E8F0"
                   BorderThickness="1">
        <ProgressBar.Style>
          <Style TargetType="ProgressBar">
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                  <Grid>
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"/>
                    <Border x:Name="PART_Track" 
                            CornerRadius="8"
                            Margin="2">
                      <Border x:Name="PART_Indicator"
                              Background="{TemplateBinding Foreground}"
                              HorizontalAlignment="Left"
                              CornerRadius="6"/>
                    </Border>
                  </Grid>
                </ControlTemplate>
              </Setter.Value>
            </Setter>
          </Style>
        </ProgressBar.Style>
      </ProgressBar>
      
      <!-- رسالة التقدم -->
      <TextBlock x:Name="MessageTextBlock"
                 Text="بدء المعالجة..." 
                 FontSize="14" 
                 Foreground="#64748B" 
                 HorizontalAlignment="Center" 
                 TextWrapping="Wrap"
                 Margin="0,0,0,15"
                 FontFamily="{StaticResource Tajawal}"/>
      
      <!-- النسبة المئوية -->
      <Border Background="#F8FAFC" 
              CornerRadius="8" 
              Padding="15,10"
              HorizontalAlignment="Center"
              BorderBrush="#E2E8F0"
              BorderThickness="1">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="التقدم: " 
                     FontSize="15" 
                     Foreground="#475569"
                     FontFamily="{StaticResource Tajawal}"/>
          <TextBlock x:Name="PercentageTextBlock"
                     Text="0%" 
                     FontSize="18" 
                     FontWeight="Bold" 
                     Foreground="#8B5CF6"
                     FontFamily="{StaticResource Tajawal}"/>
        </StackPanel>
      </Border>
    </StackPanel>
  </Border>
</Window>
