<Window x:Class="Alwajeih.Views.Notifications.ToastNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="إشعار" 
        Height="Auto" 
        Width="360"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        FontFamily="pack://application:,,,/Resources/Fonts/#Tajawal"
        Loaded="Window_Loaded">
    
    <Window.Resources>
        <Storyboard x:Key="ShowAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetProperty="(Window.Left)"
                           From="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=ActualWidth}"
                           Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        
        <Storyboard x:Key="HideAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                           From="1" To="0" Duration="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetProperty="(Window.Left)"
                           Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
    </Window.Resources>
    
    <Border Background="White" 
            CornerRadius="12" 
            BorderBrush="#E5E7EB" 
            BorderThickness="1"
            Margin="15"
            Padding="0">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="25" ShadowDepth="2" Opacity="0.25" Direction="270"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Border Grid.Row="0" 
                    Background="{Binding HeaderColor}" 
                    CornerRadius="12,12,0,0" 
                    Padding="14,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" 
                               Text="{Binding Icon}" 
                               FontSize="20" 
                               VerticalAlignment="Center"
                               Margin="0,0,10,0"/>
                    
                    <TextBlock Grid.Column="1" 
                               Text="{Binding Title}" 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Foreground="White"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                    
                    <Button Grid.Column="2"
                            Content="✕"
                            FontSize="16"
                            FontWeight="Bold"
                            Foreground="White"
                            Background="Transparent"
                            BorderThickness="0"
                            Width="28"
                            Height="28"
                            Cursor="Hand"
                            Click="CloseButton_Click"
                            ToolTip="إغلاق">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="6"
                                                    Width="{TemplateBinding Width}"
                                                    Height="{TemplateBinding Height}">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#40FFFFFF"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#60FFFFFF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
            
            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="14,12" Background="White">
                <TextBlock Text="{Binding Message}" 
                           FontSize="13" 
                           FontWeight="Medium"
                           Foreground="#1F2937"
                           TextWrapping="Wrap"
                           LineHeight="20"
                           TextAlignment="Right"/>
                           
                <TextBlock Text="{Binding SubMessage}" 
                           FontSize="12" 
                           Foreground="#6B7280"
                           TextWrapping="Wrap"
                           LineHeight="18"
                           Margin="0,6,0,0"
                           TextAlignment="Right"
                           Visibility="{Binding SubMessageVisibility}"/>
            </StackPanel>
            
            <!-- Actions -->
            <Border Grid.Row="2" 
                    Background="#F9FAFB"
                    BorderBrush="#E5E7EB"
                    BorderThickness="0,1,0,0"
                    CornerRadius="0,0,12,12"
                    Visibility="{Binding ActionsVisibility}">
                <StackPanel Orientation="Horizontal" 
                           HorizontalAlignment="Left"
                           Margin="14,10">
                    <Button Content="{Binding SecondaryActionText}"
                            Background="#F3F4F6"
                            Foreground="#4B5563"
                            BorderThickness="1"
                            BorderBrush="#D1D5DB"
                            Padding="16,8"
                            Margin="0,0,8,0"
                            FontSize="12"
                            FontWeight="Medium"
                            Cursor="Hand"
                            Click="SecondaryAction_Click"
                            Visibility="{Binding SecondaryActionVisibility}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="8"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#E5E7EB"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#D1D5DB"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    
                    <Button Content="{Binding PrimaryActionText}"
                            Background="{Binding HeaderColor}"
                            Foreground="White"
                            BorderThickness="0"
                            Padding="16,8"
                            FontSize="12"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            Click="PrimaryAction_Click"
                            Visibility="{Binding PrimaryActionVisibility}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="8"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Opacity" Value="0.9"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Opacity" Value="0.8"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
