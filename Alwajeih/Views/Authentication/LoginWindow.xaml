<Window x:Class="Alwajeih.Views.Authentication.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewmodels="clr-namespace:Alwajeih.ViewModels.Authentication"
        xmlns:converters="clr-namespace:Alwajeih.Utilities.Converters"
        Title="تسجيل الدخول - الوجيه" 
        Height="650" Width="1100"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FlowDirection="RightToLeft">
    
    <Window.DataContext>
        <viewmodels:LoginViewModel/>
    </Window.DataContext>
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles/Colors.xaml"/>
                <ResourceDictionary Source="/Resources/Styles/Buttons.xaml"/>
                <ResourceDictionary Source="/Resources/Styles/TextBoxes.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        </ResourceDictionary>
    </Window.Resources>
    
    <Border CornerRadius="16" Background="White">
        <Border.Effect>
            <DropShadowEffect Color="#1E40AF" BlurRadius="40" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            
            <!-- الجانب الأيمن - تصميم جذاب بخلفية فاتحة -->
            <Border Grid.Column="0" CornerRadius="16,0,0,16">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#EFF6FF" Offset="0"/>
                        <GradientStop Color="#DBEAFE" Offset="0.5"/>
                        <GradientStop Color="#BFDBFE" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <!-- شعار كبير مع تصميم سعودي -->
                <Viewbox Width="250" Height="250">
                    <Grid>
                        <!-- دائرة خارجية محسنة -->
                        <Ellipse Width="250" Height="250" Stroke="#3B82F6" StrokeThickness="3" Fill="Transparent"/>
                        
                        <!-- دائرة داخلية بـ Gradient -->
                        <Ellipse Width="230" Height="230">
                            <Ellipse.Fill>
                                <RadialGradientBrush>
                                    <GradientStop Color="#3B82F6" Offset="0"/>
                                    <GradientStop Color="#2563EB" Offset="0.7"/>
                                    <GradientStop Color="#1E40AF" Offset="1"/>
                                </RadialGradientBrush>
                            </Ellipse.Fill>
                            <Ellipse.Effect>
                                <DropShadowEffect Color="#2563EB" BlurRadius="30" ShadowDepth="0" Opacity="0.4"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        
                        <!-- المحتوى -->
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="💰" FontSize="80" HorizontalAlignment="Center">
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="White" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
                                </TextBlock.Effect>
                            </TextBlock>
                            <TextBlock Text="الوجيه" 
                                     FontSize="50" 
                                     FontWeight="Bold" 
                                     Foreground="White"
                                     HorizontalAlignment="Center"
                                     FontFamily="{StaticResource Tajawal}">
                                <TextBlock.Effect>
                                    <DropShadowEffect Color="#1E40AF" BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                                </TextBlock.Effect>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </Viewbox>
                
                <TextBlock Text="نظام إدارة الادخار" 
                         FontSize="28" 
                         FontWeight="SemiBold"
                         Foreground="#1E40AF" 
                         HorizontalAlignment="Center" 
                         Margin="0,35,0,12"
                         FontFamily="{StaticResource Tajawal}"/>
                
                <TextBlock Text="نظام متكامل لإدارة دورات الادخار" 
                         FontSize="15" 
                         Foreground="#64748B" 
                         HorizontalAlignment="Center" 
                         TextWrapping="Wrap"
                         TextAlignment="Center"
                         Width="320"
                         LineHeight="24"
                         FontFamily="{StaticResource Tajawal}"/>
                
                <!-- Features -->
                <StackPanel Margin="0,25,0,0" HorizontalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="✓" FontSize="16" Foreground="#10B981" Margin="0,0,8,0"/>
                        <TextBlock Text="إدارة الأعضاء والحصص" FontSize="14" Foreground="#475569" FontFamily="{StaticResource Tajawal}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="✓" FontSize="16" Foreground="#10B981" Margin="0,0,8,0"/>
                        <TextBlock Text="التحصيل اليومي والمتأخرات" FontSize="14" Foreground="#475569" FontFamily="{StaticResource Tajawal}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="✓" FontSize="16" Foreground="#10B981" Margin="0,0,8,0"/>
                        <TextBlock Text="التقارير والإيصالات" FontSize="14" Foreground="#475569" FontFamily="{StaticResource Tajawal}"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>
        
        <!-- الجانب الأيسر - نموذج تسجيل الدخول -->
        <Border Grid.Column="1" Background="White" CornerRadius="0,16,16,0" Padding="60,50">
            <StackPanel VerticalAlignment="Center" MaxWidth="380">
                
                <!-- زر الإغلاق -->
                <Button Content="✕" 
                        Width="32" Height="32"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Margin="-50,-40,0,0"
                        Background="Transparent"
                        Foreground="#94A3B8"
                        BorderThickness="0"
                        FontSize="20"
                        Cursor="Hand"
                        Click="CloseButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F1F5F9"/>
                                    <Setter Property="Foreground" Value="#1E40AF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <!-- العنوان -->
                <TextBlock Text="مرحباً بك 👋" 
                         FontSize="36" 
                         FontWeight="Bold" 
                         Foreground="#1E293B" 
                         HorizontalAlignment="Center"
                         FontFamily="{StaticResource Tajawal}"/>
                
                <TextBlock Text="قم بتسجيل الدخول إلى حسابك" 
                         FontSize="15" 
                         Foreground="#64748B" 
                         HorizontalAlignment="Center" 
                         Margin="0,8,0,45"
                         FontFamily="{StaticResource Tajawal}"/>
                
                <!-- حقل اسم المستخدم -->
                <Label Content="اسم المستخدم" 
                       Foreground="#1E293B" 
                       FontSize="14" 
                       FontWeight="Medium"
                       Margin="0,0,0,8"
                       FontFamily="{StaticResource Tajawal}"/>
                
                <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Height="48">
                    <TextBox x:Name="UsernameTextBox"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="16,0"
                             VerticalContentAlignment="Center"
                             FontSize="15"
                             Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                             FontFamily="{StaticResource Tajawal}"/>
                </Border>
                
                <!-- حقل كلمة المرور -->
                <Label Content="كلمة المرور" 
                       Foreground="#1E293B" 
                       FontSize="14" 
                       FontWeight="Medium"
                       Margin="0,20,0,8"
                       FontFamily="{StaticResource Tajawal}"/>
                
                <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Height="48">
                    <PasswordBox x:Name="PasswordBox"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="16,0"
                                 VerticalContentAlignment="Center"
                                 FontSize="15"
                                 PasswordChanged="PasswordBox_PasswordChanged"
                                 FontFamily="{StaticResource Tajawal}"/>
                </Border>
                
                <!-- رسالة الخطأ -->
                <Border Background="#FEF2F2" 
                        BorderBrush="#FCA5A5" 
                        BorderThickness="1" 
                        CornerRadius="6" 
                        Padding="12,10"
                        Margin="0,16,0,0"
                        Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚠️" FontSize="14" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding ErrorMessage}" 
                                 Foreground="#DC2626" 
                                 FontSize="13" 
                                 TextWrapping="Wrap"
                                 FontFamily="{StaticResource Tajawal}"/>
                    </StackPanel>
                </Border>
                
                <!-- زر تسجيل الدخول -->
                <Button Content="تسجيل الدخول 🔐" 
                        Height="52" 
                        Margin="0,28,0,0"
                        Command="{Binding LoginCommand}"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        FontFamily="{StaticResource Tajawal}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#2563EB" Offset="0"/>
                                        <GradientStop Color="#1E40AF" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                CornerRadius="8"
                                                BorderThickness="0">
                                            <Border.Effect>
                                                <DropShadowEffect Color="#2563EB" BlurRadius="15" ShadowDepth="0" Opacity="0.3"/>
                                            </Border.Effect>
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                <GradientStop Color="#1E40AF" Offset="0"/>
                                                <GradientStop Color="#1E3A8A" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <!-- معلومات إضافية -->
                <StackPanel Orientation="Horizontal" 
                          HorizontalAlignment="Center" 
                          Margin="0,30,0,0">
                    <TextBlock Text="المستخدم الافتراضي: " 
                             Foreground="{StaticResource TextSecondaryColor}" 
                             FontSize="12"
                             FontFamily="{StaticResource Tajawal}"/>
                    <TextBlock Text="admin" 
                             Foreground="{StaticResource PrimaryColor}" 
                             FontSize="12" 
                             FontWeight="Bold"
                             FontFamily="{StaticResource Tajawal}"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" 
                          HorizontalAlignment="Center" 
                          Margin="0,5,0,0">
                    <TextBlock Text="كلمة المرور: " 
                             Foreground="{StaticResource TextSecondaryColor}" 
                             FontSize="12"
                             FontFamily="{StaticResource Tajawal}"/>
                    <TextBlock Text="admin123" 
                             Foreground="{StaticResource PrimaryColor}" 
                             FontSize="12" 
                             FontWeight="Bold"
                             FontFamily="{StaticResource Tajawal}"/>
                </StackPanel>
            </StackPanel>
        </Border>
        </Grid>
    </Border>
</Window>
