<UserControl
  x:Class="Alwajeih.Views.BehindAssociation.BehindAssociationView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:vm="clr-namespace:Alwajeih.ViewModels.BehindAssociation"
  xmlns:converters="clr-namespace:Alwajeih.Utilities.Converters"
  FlowDirection="RightToLeft"
  FontFamily="{StaticResource Tajawal}"
  FontSize="14"
>
  <UserControl.DataContext>
    <vm:BehindAssociationViewModel />
  </UserControl.DataContext>
  
  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Resources/Styles/ScrollBarStyles.xaml" />
        <ResourceDictionary Source="/Resources/Styles/Colors.xaml"/>
        <ResourceDictionary Source="/Resources/Styles/Buttons.xaml"/>
        <ResourceDictionary Source="/Resources/Styles/MainStyles.xaml"/>
        <ResourceDictionary Source="/Resources/Styles/DataGrids.xaml"/>
      </ResourceDictionary.MergedDictionaries>
      
      <!-- Converters -->
      <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
      <converters:PaymentSourceConverter x:Key="PaymentSourceConverter" />
    </ResourceDictionary>
  </UserControl.Resources>

  <Grid Background="#F8FAFC">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- العنوان -->
    <StackPanel Grid.Row="0" Margin="30,25,30,20">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="0">
          <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
            <TextBlock Text="💰" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center" />
            <TextBlock
              Text="إدارة أعضاء خلف الجمعية"
              FontSize="22"
              FontWeight="Bold"
              Foreground="#1E293B"
              VerticalAlignment="Center"
            />
          </StackPanel>
          <TextBlock
            Text="نظام أمانة - دفع وسحب متى أرادوا"
            FontSize="13" Foreground="#64748B" Margin="34,0,0,0"
          />
        </StackPanel>
        
        <!-- زر تسجيل دفعة جديدة -->
        <Button Grid.Column="1"
                Command="{Binding OpenDepositDialogCommand}"
                Height="45"
                Padding="20,0"
                Background="#10B981"
                BorderBrush="#059669"
                BorderThickness="1"
                Cursor="Hand">
          <Button.Style>
            <Style TargetType="Button">
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                      <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                      <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#059669"/>
                      </Trigger>
                      <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.5"/>
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
            </Style>
          </Button.Style>
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="➕" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <TextBlock Text="تسجيل دفعة جديدة" FontSize="14" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center"/>
          </StackPanel>
        </Button>
      </Grid>
    </StackPanel>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <StackPanel Margin="30,0,30,30">
        
        <!-- بطاقات الإحصائيات -->
        <Border Background="White" CornerRadius="12" Padding="20" Margin="0,0,0,20">
          <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="0" Opacity="0.05" />
          </Border.Effect>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- عدد الأعضاء -->
            <Border Grid.Column="0" Background="#EFF6FF" CornerRadius="10" Padding="20" Margin="0,0,10,0" BorderBrush="#BFDBFE" BorderThickness="1">
              <StackPanel>
                <TextBlock Text="👥" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="عدد الأعضاء" FontSize="13" Foreground="#1E40AF" HorizontalAlignment="Center" Margin="0,0,0,8" FontFamily="{StaticResource Tajawal}"/>
                <TextBlock Text="{Binding MembersCount}" FontSize="24" FontWeight="Bold" Foreground="#2563EB" HorizontalAlignment="Center"/>
              </StackPanel>
            </Border>
            
            <!-- إجمالي الإيداعات -->
            <Border Grid.Column="1" Background="#DCFCE7" CornerRadius="10" Padding="20" Margin="0,0,10,0" BorderBrush="#86EFAC" BorderThickness="1">
              <StackPanel>
                <TextBlock Text="⬇️" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="إجمالي الإيداعات" FontSize="13" Foreground="#15803D" HorizontalAlignment="Center" Margin="0,0,0,8" FontFamily="{StaticResource Tajawal}"/>
                <TextBlock FontSize="24" FontWeight="Bold" Foreground="#10B981" HorizontalAlignment="Center">
                  <Run Text="{Binding TotalDeposits, StringFormat=N2, Mode=OneWay}"/>
                  <Run Text=" ريال" FontSize="13" Foreground="#64748B"/>
                </TextBlock>
              </StackPanel>
            </Border>
            
            <!-- إجمالي السحوبات -->
            <Border Grid.Column="2" Background="#FEF2F2" CornerRadius="10" Padding="20" Margin="0,0,10,0" BorderBrush="#FCA5A5" BorderThickness="1">
              <StackPanel>
                <TextBlock Text="⬆️" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="إجمالي السحوبات" FontSize="13" Foreground="#7F1D1D" HorizontalAlignment="Center" Margin="0,0,0,8" FontFamily="{StaticResource Tajawal}"/>
                <TextBlock FontSize="24" FontWeight="Bold" Foreground="#EF4444" HorizontalAlignment="Center">
                  <Run Text="{Binding TotalWithdrawals, StringFormat=N2, Mode=OneWay}"/>
                  <Run Text=" ريال" FontSize="13" Foreground="#64748B"/>
                </TextBlock>
              </StackPanel>
            </Border>
            
            <!-- الرصيد الكلي -->
            <Border Grid.Column="3" Background="#FEF3C7" CornerRadius="10" Padding="20" BorderBrush="#FDE68A" BorderThickness="1">
              <StackPanel>
                <TextBlock Text="💰" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="الرصيد الكلي" FontSize="13" Foreground="#92400E" HorizontalAlignment="Center" Margin="0,0,0,8" FontFamily="{StaticResource Tajawal}" TextWrapping="Wrap" TextAlignment="Center"/>
                <TextBlock FontSize="24" FontWeight="Bold" Foreground="#F59E0B" HorizontalAlignment="Center">
                  <Run Text="{Binding TotalBalance, StringFormat=N2, Mode=OneWay}"/>
                  <Run Text=" ريال" FontSize="13" Foreground="#64748B"/>
                </TextBlock>
              </StackPanel>
            </Border>
          </Grid>
        </Border>

        <!-- شريط البحث والتحديث -->
        <Border Background="White" CornerRadius="12" Padding="20" Margin="0,0,0,20">
          <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="0" Opacity="0.05" />
          </Border.Effect>
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- بحث -->
            <TextBox
              Grid.Column="0"
              Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
              Style="{StaticResource ModernTextBox}"
              Height="45"
              FontSize="14"
              Margin="0,0,15,0"
              FontFamily="{StaticResource Tajawal}"
            >
              <TextBox.Resources>
                <Style TargetType="TextBox" BasedOn="{StaticResource ModernTextBox}">
                  <Style.Triggers>
                    <Trigger Property="Text" Value="">
                      <Setter Property="Background">
                        <Setter.Value>
                          <VisualBrush Stretch="None" AlignmentX="Right">
                            <VisualBrush.Visual>
                              <TextBlock
                                Text="🔍 بحث عن عضو..."
                                Foreground="#94A3B8"
                                FontSize="14"
                                Margin="10,0"
                                FontFamily="{StaticResource Tajawal}"
                              />
                            </VisualBrush.Visual>
                          </VisualBrush>
                        </Setter.Value>
                      </Setter>
                    </Trigger>
                  </Style.Triggers>
                </Style>
              </TextBox.Resources>
            </TextBox>

            <!-- زر التحديث -->
            <Button
              Grid.Column="1"
              Command="{Binding RefreshCommand}"
              Style="{StaticResource PrimaryButton}"
              Width="120"
              Height="45"
            >
              <StackPanel Orientation="Horizontal" FlowDirection="RightToLeft">
                <TextBlock Text="🔄" FontSize="16" Margin="0,0,8,0" />
                <TextBlock Text="تحديث" FontSize="14" FontFamily="{StaticResource Tajawal}" />
              </StackPanel>
            </Button>
          </Grid>
        </Border>

        <!-- جدول الأعضاء -->
        <Border Background="White" CornerRadius="12" Padding="25" BorderBrush="#E2E8F0" BorderThickness="1">
          <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="0" Opacity="0.08" />
          </Border.Effect>
          <StackPanel>
            <TextBlock
              Text="📋 قائمة أعضاء خلف الجمعية"
              FontSize="18"
              FontWeight="SemiBold"
              Foreground="#1E293B"
              Margin="0,0,0,20"
              FontFamily="{StaticResource Tajawal}"
            />

            <DataGrid
              ItemsSource="{Binding MembersSummaries}"
              SelectedItem="{Binding SelectedMember}"
              AutoGenerateColumns="False"
              IsReadOnly="True"
              Style="{StaticResource ModernDataGrid}"
              MaxHeight="500"
              FontFamily="{StaticResource Tajawal}"
              FontSize="14"
              FontWeight="Medium"
            >
              <DataGrid.Columns>
                <DataGridTemplateColumn Header="#" Width="50">
                  <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                      <TextBlock
                        Text="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Header}"
                        HorizontalAlignment="Center"
                        FontWeight="SemiBold"
                      />
                    </DataTemplate>
                  </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                <DataGridTextColumn Header="اسم العضو" Binding="{Binding MemberName}" Width="2*"/>
                <DataGridTextColumn Header="الهاتف" Binding="{Binding Phone}" Width="1.2*"/>
                
                <DataGridTextColumn Header="إجمالي الإيداعات" Binding="{Binding TotalDeposits, StringFormat=N2}" Width="1.3*">
                  <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                      <Setter Property="Foreground" Value="#10B981"/>
                      <Setter Property="FontWeight" Value="SemiBold"/>
                    </Style>
                  </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <DataGridTextColumn Header="إجمالي السحوبات" Binding="{Binding TotalWithdrawals, StringFormat=N2}" Width="1.3*">
                  <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                      <Setter Property="Foreground" Value="#EF4444"/>
                      <Setter Property="FontWeight" Value="SemiBold"/>
                    </Style>
                  </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <DataGridTextColumn Header="الرصيد الحالي" Binding="{Binding CurrentBalance, StringFormat=N2}" Width="1.3*">
                  <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                      <Setter Property="Foreground" Value="{Binding BalanceColor}"/>
                      <Setter Property="FontWeight" Value="Bold"/>
                    </Style>
                  </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <DataGridTextColumn Header="عدد المعاملات" Binding="{Binding TransactionCount}" Width="1*">
                  <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                      <Setter Property="HorizontalAlignment" Value="Center"/>
                    </Style>
                  </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
              </DataGrid.Columns>
            </DataGrid>

            <!-- معاملات العضو المختار -->
            <Border Margin="0,20,0,0" Visibility="{Binding SelectedMember, Converter={StaticResource NullToVisibilityConverter}}">
              <StackPanel>
                <TextBlock
                  FontSize="16"
                  FontWeight="SemiBold"
                  Foreground="#1E293B"
                  Margin="0,0,0,15"
                  FontFamily="{StaticResource Tajawal}"
                >
                  <Run Text="📊 معاملات العضو: "/>
                  <Run Text="{Binding SelectedMember.MemberName, Mode=OneWay}"/>
                </TextBlock>

                <DataGrid
                  ItemsSource="{Binding SelectedMemberTransactions}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Style="{StaticResource ModernDataGrid}"
                  MaxHeight="300"
                  FontFamily="{StaticResource Tajawal}"
                  FontSize="13"
                >
                  <DataGrid.Columns>
                    <DataGridTextColumn Header="التاريخ" Binding="{Binding TransactionDate, StringFormat=dd/MM/yyyy}" Width="*"/>
                    <DataGridTextColumn Header="النوع" Binding="{Binding TransactionTypeDisplay}" Width="*"/>
                    <DataGridTextColumn Header="المبلغ" Binding="{Binding Amount, StringFormat=N2}" Width="1.2*">
                      <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                          <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                      </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="المصدر" Binding="{Binding PaymentSource, Converter={StaticResource PaymentSourceConverter}}" Width="*"/>
                    <DataGridTextColumn Header="رقم المرجع" Binding="{Binding ReferenceNumber}" Width="1.2*"/>
                    <DataGridTextColumn Header="ملاحظات" Binding="{Binding Notes}" Width="2*"/>
                  </DataGrid.Columns>
                </DataGrid>
              </StackPanel>
            </Border>
          </StackPanel>
        </Border>

      </StackPanel>
    </ScrollViewer>

    <!-- نافذة تسجيل دفعة منبثقة -->
    <Border
      Grid.Row="0" Grid.RowSpan="2"
      Background="#80000000"
      Visibility="{Binding IsDepositDialogOpen, Converter={StaticResource BooleanToVisibilityConverter}}"
    >
      <Border
        Background="White"
        CornerRadius="12"
        MaxWidth="500"
        Padding="30"
        VerticalAlignment="Center"
        HorizontalAlignment="Center"
      >
        <Border.Effect>
          <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="0" Opacity="0.3" />
        </Border.Effect>
        
        <StackPanel>
          <TextBlock Text="➕ تسجيل دفعة جديدة" FontSize="20" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,20" HorizontalAlignment="Center"/>
          
          <!-- اختيار العضو -->
          <TextBlock Text="العضو:" FontSize="14" FontWeight="Medium" Margin="0,0,0,5"/>
          <ComboBox
            ItemsSource="{Binding BehindAssociationMembers}"
            SelectedValue="{Binding SelectedMemberId}"
            SelectedValuePath="MemberID"
            DisplayMemberPath="Name"
            Style="{StaticResource ModernComboBox}"
            Height="45"
            Margin="0,0,0,15"
          />
          
          <!-- المبلغ -->
          <TextBlock Text="المبلغ:" FontSize="14" FontWeight="Medium" Margin="0,0,0,5"/>
          <TextBox
            Text="{Binding DepositAmount}"
            Style="{StaticResource ModernTextBox}"
            Height="45"
            Margin="0,0,0,15"
          />
          
          <!-- مصدر الدفع -->
          <TextBlock Text="وسيلة الدفع:" FontSize="14" FontWeight="Medium" Margin="0,0,0,5"/>
          <ComboBox
            ItemsSource="{Binding PaymentSources}"
            SelectedItem="{Binding SelectedPaymentSource}"
            Style="{StaticResource ModernComboBox}"
            Height="45"
            Margin="0,0,0,15"
          >
            <ComboBox.ItemTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding Converter={StaticResource PaymentSourceConverter}}" />
              </DataTemplate>
            </ComboBox.ItemTemplate>
          </ComboBox>
          
          <!-- الرقم المرجعي -->
          <TextBlock Text="الرقم المرجعي (اختياري):" FontSize="14" FontWeight="Medium" Margin="0,0,0,5"/>
          <TextBox
            Text="{Binding ReferenceNumber}"
            Style="{StaticResource ModernTextBox}"
            Height="45"
            Margin="0,0,0,15"
          />
          
          <!-- ملاحظات -->
          <TextBlock Text="ملاحظات (اختياري):" FontSize="14" FontWeight="Medium" Margin="0,0,0,5"/>
          <TextBox
            Text="{Binding Notes}"
            Style="{StaticResource ModernTextBox}"
            Height="80"
            TextWrapping="Wrap"
            AcceptsReturn="True"
            VerticalScrollBarVisibility="Auto"
            Margin="0,0,0,20"
          />
          
          <!-- أزرار -->
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="10"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0" Content="حفظ" Command="{Binding RecordDepositCommand}" Style="{StaticResource PrimaryButton}" Height="45"/>
            <Button Grid.Column="2" Content="إلغاء" Command="{Binding CancelDepositCommand}" Style="{StaticResource SecondaryButton}" Height="45"/>
          </Grid>
        </StackPanel>
      </Border>
    </Border>
  </Grid>
</UserControl>
