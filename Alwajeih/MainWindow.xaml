<Window
  x:Class="Alwajeih.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
  Title="الوجيه - نظام إدارة الادخار"
  Height="800"
  Width="1400"
  WindowState="Maximized"
  WindowStartupLocation="CenterScreen"
  FlowDirection="RightToLeft"
  FontFamily="{StaticResource Tajawal}"
>
  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Resources/Styles/ScrollBarStyles.xaml"/>
      </ResourceDictionary.MergedDictionaries>
      
    <!-- نمط أزرار القائمة الجانبية -->
    <Style x:Key="SideMenuButton" TargetType="Button">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Foreground" Value="#475569" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="HorizontalAlignment" Value="Stretch" />
      <Setter Property="HorizontalContentAlignment" Value="Right" />
      <Setter Property="Padding" Value="16,10" />
      <Setter Property="Margin" Value="8,3" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="FontWeight" Value="Medium" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Height" Value="44" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border
              x:Name="border"
              Background="{TemplateBinding Background}"
              BorderBrush="{TemplateBinding BorderBrush}"
              BorderThickness="{TemplateBinding BorderThickness}"
              CornerRadius="10"
              Padding="{TemplateBinding Padding}"
            >
              <ContentPresenter
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="Center"
              />
            </Border>
            <ControlTemplate.Triggers>
              <!-- تأثير Hover -->
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="border" Property="Background" Value="#F1F5F9" />
              </Trigger>
              <!-- تأثير الضغط -->
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="border" Property="Background" Value="#E2E8F0" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <!-- نمط الزر المحدد/النشط -->
    <Style
      x:Key="SideMenuButtonActive"
      TargetType="Button"
      BasedOn="{StaticResource SideMenuButton}"
    >
      <Setter Property="Background" Value="#EFF6FF" />
      <Setter Property="Foreground" Value="#2563EB" />
      <Setter Property="FontWeight" Value="Bold" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border
              x:Name="border"
              Background="{TemplateBinding Background}"
              BorderBrush="#BFDBFE"
              BorderThickness="1"
              CornerRadius="10"
              Padding="{TemplateBinding Padding}"
            >
              <Border.Effect>
                <DropShadowEffect Color="#3B82F6" BlurRadius="8" ShadowDepth="0" Opacity="0.15" />
              </Border.Effect>
              <ContentPresenter
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                VerticalAlignment="Center"
              />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="border" Property="Background" Value="#DBEAFE" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="border" Property="Background" Value="#BFDBFE" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    </ResourceDictionary>
  </Window.Resources>
  <Grid Background="{StaticResource BackgroundColor}">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" MinWidth="220" MaxWidth="280" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <!-- القائمة الجانبية -->
    <Border
      Grid.Column="0"
      Background="White"
      BorderThickness="0,0,1,0"
      BorderBrush="#E2E8F0"
    >
      <Border.Effect>
        <DropShadowEffect Color="#1E40AF" BlurRadius="10" ShadowDepth="2" Opacity="0.05" />
      </Border.Effect>
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!-- مربع الشعار والاسم -->
        <Border
          Grid.Row="0"
          Background="#F8FAFC"
          Padding="20,25"
          BorderThickness="0,0,0,1"
          BorderBrush="#E2E8F0"
        >
          <StackPanel HorizontalAlignment="Center">
            <!-- شعار دائري -->
            <Border
              Width="85"
              Height="85"
              CornerRadius="43"
              Margin="0,0,0,18"
              HorizontalAlignment="Center"
            >
              <Border.Background>
                <RadialGradientBrush>
                  <GradientStop Color="#3B82F6" Offset="0" />
                  <GradientStop Color="#2563EB" Offset="1" />
                </RadialGradientBrush>
              </Border.Background>
              <Border.Effect>
                <DropShadowEffect Color="#2563EB" BlurRadius="20" ShadowDepth="0" Opacity="0.3" />
              </Border.Effect>
              <TextBlock
                Text="💰"
                FontSize="48"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Foreground="White"
              />
            </Border>
            <!-- اسم النظام -->
            <TextBlock
              Text="الوجيه"
              FontSize="24"
              FontWeight="Bold"
              Foreground="#1E293B"
              TextAlignment="Center"
              Margin="0,0,0,6"
            />
            <!-- الوصف -->
            <TextBlock
              Text="نظام إدارة الادخار"
              FontSize="13"
              Foreground="#64748B"
              TextAlignment="Center"
            />
          </StackPanel>
        </Border>
        <!-- القائمة -->
        <ScrollViewer 
          Grid.Row="1" 
          VerticalScrollBarVisibility="Auto" 
          HorizontalScrollBarVisibility="Disabled"
          Padding="0,0,0,0"
          Style="{StaticResource ThinScrollViewer}"
        >
          <StackPanel Margin="0,10,0,10">
            <!-- أزرار القائمة مع إيموجي -->
            <Button
              x:Name="DashboardButton"
              Style="{StaticResource SideMenuButtonActive}"
              Click="MenuButton_Click"
              Tag="Dashboard"
              HorizontalContentAlignment="Left"
            >
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="📊" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center" />
                <TextBlock Text="لوحة التحكم" FontSize="14" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
            <Button
              x:Name="MembersButton"
              Style="{StaticResource SideMenuButton}"
              Click="MenuButton_Click"
              Tag="Members"
              HorizontalContentAlignment="Left"
            >
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="👥" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center" />
                <TextBlock Text="إدارة الأعضاء" FontSize="14" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
            <Button
              x:Name="PlansButton"
              Style="{StaticResource SideMenuButton}"
              Click="MenuButton_Click"
              Tag="Plans"
              HorizontalContentAlignment="Left"
            >
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="📈" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center" />
                <TextBlock Text="إدارة الأسهم" FontSize="14" VerticalAlignment="Center" />
              </StackPanel>
            </Button>

             <Button
              x:Name="BehindAssociationButton"
              Style="{StaticResource SideMenuButton}"
              Click="MenuButton_Click"
              Tag="BehindAssociation"
              HorizontalContentAlignment="Left"
            >
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="💰" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center" />
                <TextBlock Text="خلف الجمعية" FontSize="14" VerticalAlignment="Center" />
            </StackPanel>
        </Button>

                        <Button
              x:Name="CollectionButton"
              Style="{StaticResource SideMenuButton}"
              Click="MenuButton_Click"
              Tag="Collection"
              HorizontalContentAlignment="Left"
            >
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="💵" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center" />
                <TextBlock Text="التحصيل" FontSize="14" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
            <Button
              x:Name="ArrearsManagementButton"
              Style="{StaticResource SideMenuButton}"
              Click="MenuButton_Click"
              Tag="ArrearsManagement"
              HorizontalContentAlignment="Left"
            >
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚠️" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center" />
                <TextBlock Text="المتأخرات والسابقات" FontSize="14" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
                        <Button
                           x:Name="ReconciliationButton"
                           Style="{StaticResource SideMenuButton}"
                           Click="MenuButton_Click"
                           Tag="Reconciliation"
                           HorizontalContentAlignment="Left"
 >
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📊" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center" />
                                <TextBlock Text="الجرد الأسبوعي" FontSize="14" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button
              x:Name="VaultButton"
              Style="{StaticResource SideMenuButton}"
              Click="MenuButton_Click"
              Tag="Vault"
              HorizontalContentAlignment="Left"
            >
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="🏦" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center" />
                <TextBlock Text="الخزنة" FontSize="14" VerticalAlignment="Center" />
              </StackPanel>
            </Button>

                        <Button
                              x:Name="ExternalPaymentButton"
                              Style="{StaticResource SideMenuButton}"
                              Click="MenuButton_Click"
                              Tag="ExternalPayment"
                              HorizontalContentAlignment="Left"
                            >   
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="💳" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center" />
                                <TextBlock Text="كريمي" FontSize="14" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>

                        <Button
              x:Name="ReportsButton"
              Style="{StaticResource SideMenuButton}"
              Click="MenuButton_Click"
              Tag="Reports"
              HorizontalContentAlignment="Left"
            >
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="📁" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center" />
                <TextBlock Text="التقارير" FontSize="14" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
            
            <Button
              x:Name="ReceiptButton"
              Style="{StaticResource SideMenuButton}"
              Click="MenuButton_Click"
              Tag="Receipt"
              HorizontalContentAlignment="Left"
            >
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="🧾" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center" />
                <TextBlock Text="الإيصالات" FontSize="14" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
            
            <!-- فاصل أنيق -->
            <Separator Margin="15,10" Background="#E2E8F0" Height="1" />
            <Button
              x:Name="SettingsButton"
              Style="{StaticResource SideMenuButton}"
              Click="MenuButton_Click"
              Tag="Settings"
              HorizontalContentAlignment="Left"
            >
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚙️" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center" />
                <TextBlock Text="الإعدادات" FontSize="14" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
            <Button
              x:Name="ArchiveButton"
              Style="{StaticResource SideMenuButton}"
              Click="MenuButton_Click"
              Tag="Archive"
              HorizontalContentAlignment="Left"
            >
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="📦" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center" />
                <TextBlock Text="الأرشفة" FontSize="14" VerticalAlignment="Center" />
              </StackPanel>
            </Button>
          </StackPanel>
        </ScrollViewer>
        <!-- معلومات المستخدم في الأسفل -->
        <Border
          Grid.Row="2"
          Background="#F8FAFC"
          BorderThickness="0,1,0,0"
          BorderBrush="#E2E8F0"
          Padding="15,12"
        >
          <StackPanel>
            <!-- معلومات المستخدم -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
              <!-- أيقونة المستخدم -->
              <Border Width="36" Height="36" Background="#EFF6FF" CornerRadius="18" Margin="0,0,10,0">
                <TextBlock
                  Text="👤"
                  FontSize="18"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                />
              </Border>
              <StackPanel VerticalAlignment="Center">
                <TextBlock
                  x:Name="UserNameTextBlock"
                  Text="المستخدم"
                  FontSize="13"
                  FontWeight="SemiBold"
                  Foreground="#1E293B"
                  TextAlignment="Right"
                />
                <TextBlock
                  x:Name="UserRoleTextBlock"
                  Text="الدور"
                  FontSize="11"
                  Foreground="#64748B"
                  TextAlignment="Right"
                />
              </StackPanel>
            </StackPanel>
            <!-- زر تسجيل الخروج -->
            <Button
              Content="تسجيل الخروج 🚪"
              Background="#FEF2F2"
              Foreground="#DC2626"
              BorderBrush="#FCA5A5"
              BorderThickness="1"
              Padding="12,8"
              FontSize="12"
              FontWeight="Medium"
              Cursor="Hand"
              Click="LogoutButton_Click"
              HorizontalAlignment="Stretch"
            />
          </StackPanel>
        </Border>
      </Grid>
    </Border>
    <!-- منطقة المحتوى الرئيسي -->
    <Frame
      x:Name="MainContentFrame"
      Grid.Column="1"
      NavigationUIVisibility="Hidden"
      Margin="20"
    />
  </Grid>
</Window>
